# Architecture

worktree-studio is a Go HTTP server + embedded React SPA for managing `git worktree`s across one or more registered repos. This doc is updated at the end of every build step in `PLAN.md`; it currently reflects **steps 1–4, the complete step 7 UI overhaul** (sidebar, command palette, Command Deck visual design, dockview terminal arrangement, terminal layout persistence), and the **per-worktree audit log viewer** (a step 6 sub-piece, ahead of the diff/comment-to-agent part of that step).

## Pieces that exist today

```
cmd/worktree-studio/main.go   entrypoint: chi router, embeds web/dist, graceful fallback page, reconciles tmux sessions at startup
web/embed.go                  go:embed all:dist (must live next to dist/, embed patterns are dir-relative)
internal/store/                SQLite registry: repos, worktrees, terminal_sessions
internal/gitops/                shells out to `git worktree add/remove/list --porcelain`, `git status`
internal/audit/                 JSONL audit logger, append-only
internal/term/                  tmux-backed terminal session manager: create/list/close, pty attach+resize, startup reconciliation
internal/spotlight/             thin os/exec wrapper around the external `spotlight` CLI (worktree->root mirroring)
internal/api/                   HTTP handlers (repos, worktrees, terminals, spotlight) + ws relay + adjective-noun name generator + slugify/id helpers
web/src/                        Vite + React + TS SPA — see "Frontend structure" below; RepoPicker/Workspace/WorktreeDetail are the routed pages, everything else is shared shell or a component
```

## Frontend structure (post-overhaul)

- **`Layout.tsx`** — the persistent app shell. Wraps all three routes via `<Outlet/>`; mounts `RepoProvider` (context), `Sidebar`, and `CommandPalette` once, above the routes, so none of them remount when navigating between pages — this app is an SPA in the literal sense, not independently-loading pages. Also owns the add-repo/new-worktree modal *state* (in its `LayoutShell` inner component), since both the sidebar's "+" buttons and the command palette need to open the same dialog instance.
- **`RepoContext.tsx`** — `RepoProvider`/`useRepoContext()`. Owns: the registered-repos list, `selectedRepoId` (derived via `useMatch("/repo/:repoId/*")`, not `useParams` — the provider is mounted *above* the matched route, so plain params wouldn't be visible there), **every** registered repo's worktrees (`worktreesByRepo: Record<repoId, Worktree[]>` — not just the selected repo's, since the sidebar nests every repo's worktrees under it, not just the current one), and a 5s poll of git+spotlight status across all of them. `Sidebar.tsx` and `Workspace.tsx` both read from this single fetch/poll — no duplicated polling.
- **`Sidebar.tsx`** — every repo, each repo's worktrees nested directly underneath as **flight-strip rows** (see `docs/design.md`): mono branch name, a colored left-edge tab for dirty/clean (`data-dirty="true"|"false"` set explicitly per row, read by CSS — not inferred from a child element), ahead/behind tick marks when applicable, a cyan dot when spotlight is active. Each worktree row's `WorktreeActionsMenu` kebab holds spotlight start/stop and delete. A "+" beside "Repos" opens `AddRepoDialog`; a "+" beside each repo name opens `NewWorktreeDialog` scoped to that repo (both via the callback props from `Layout.tsx`, not owned locally).
- **`CommandPalette.tsx`** — Cmd/Ctrl+K, built on `cmdk` (headless). Fuzzy-jumps to any repo/worktree from `RepoContext`, or triggers the same add-repo/new-worktree dialogs. Scoped deliberately narrow (jump/create only) — not arbitrary command execution.
- **`WorktreeActionsMenu.tsx`** — the shared kebab-menu component behind every per-worktree action, used by both `Sidebar.tsx` and `WorktreeList.tsx` (which additionally gets an "Open" item, since its rows aren't themselves nav links the way sidebar rows are). Its click handler must call **both** `preventDefault()` and `stopPropagation()` — a real bug, once shipped: when this menu is nested inside a `<Link>` (the sidebar row), `stopPropagation()` alone prevents the Link's own navigation handler from running, but does *not* suppress the anchor's native default action, so the click fell through to a full-page reload. Fixed and covered by a regression test (`WorktreeActionsMenu.test.tsx`) that dispatches a real click and checks `event.defaultPrevented`.
- **`worktreeActions.ts`** — shared delete-with-confirm-and-force-retry / spotlight-start-with-friendly-conflict-message logic, used by both `Sidebar.tsx` and `WorktreeList.tsx` so that flow isn't duplicated.
- **`WorktreeDetail.tsx`** — terminal arrangement, via **dockview** (`dockview-react`), not a plain tab strip. See "Terminal arrangement" below.
- **`Terminal.tsx`** — unchanged by the whole overhaul. Still just xterm.js + a websocket to `/ws/terminals/:id`; hosting it inside a dockview panel instead of a manually-toggled `display:none` div doesn't touch this component at all.

### Request flow

1. Browser loads `/`. `RepoProvider` (mounted in `Layout.tsx`, above every route) calls `GET /api/repos/` once, then `GET /api/repos/:id/worktrees/` for every repo returned, then per-worktree `GET .../status` and `GET .../spotlight/` — all fanned out via `Promise.all`, refetched every 5s. `Sidebar.tsx` renders this data as a tree; `RepoPicker.tsx` (the page under `/`) is just an empty-state message now, not a list of its own.
2. Adding a repo is a modal (`AddRepoDialog.tsx`, opened from the sidebar's "+" or the command palette) — `POST /api/repos/` with `{name, path}`. The handler validates the path is a real directory and a real git repo (`git rev-parse --is-inside-work-tree`) via `internal/gitops.IsGitRepo`, persists it via `internal/store`, appends a `repo.add` audit line, and the frontend navigates straight to `/repo/:id` on success.
3. Clicking a repo (in the sidebar, or via the command palette) navigates to `/repo/:id` (`Workspace.tsx`) — only the `<Outlet/>` content swaps, the sidebar doesn't remount or refetch. `Workspace.tsx` reads its worktree list from `RepoContext` (`worktreesByRepo[repoId]`), it doesn't fetch its own.
4. Creating a worktree is also a modal (`NewWorktreeDialog.tsx`, opened from the sidebar's per-repo "+", the command palette, or Workspace's own "+ New worktree" button) — immediately calls `GET /api/repos/:repoId/worktrees/new-name-suggestion` to prefill an editable adjective-noun name (e.g. `amber-ridge`) from a small embedded Go wordlist (`internal/api/wordlist.go`). Submitting calls `POST /api/repos/:repoId/worktrees/` with `{name}`; the handler slugifies the (possibly user-edited) name, uses it as both branch name and directory name, and runs `git worktree add -b <branch> <path>` against the registered repo's path — creating the worktree at `~/.worktree-studio/worktrees/<repoId>/<slug>`, deliberately **outside** the source repo tree. Persists a `worktrees` row and appends a `worktree.create` audit line. When triggered from the sidebar or palette, the frontend navigates straight into the new worktree's terminal view.
5. Deleting a worktree (via its row's kebab menu — `WorktreeActionsMenu.tsx`, shared by `Sidebar.tsx` and `WorktreeList.tsx`) calls `DELETE /api/repos/:repoId/worktrees/:worktreeId`. The handler first closes every terminal session for that worktree (real `tmux kill-session` via `internal/term.Manager.CloseSession` — added after a real bug was found: the tmux process was never killed at all, and — a second bug found right after — the `terminal_sessions`/`worktree_layouts` DB rows weren't actually cascade-deleted either, since SQLite doesn't enforce `ON DELETE CASCADE` without `PRAGMA foreign_keys = ON`, which nothing had ever set; see "Storage" below), then runs `git worktree remove --force <path>` (or without `--force`, surfacing a `409` if the worktree is dirty — see the frontend's confirm-then-retry-with-force flow), deletes the DB row, and appends a `worktree.remove` audit line.
6. Clicking a worktree (sidebar row, "Open" in `WorktreeList.tsx`, or the command palette) navigates to `/repo/:repoId/worktree/:worktreeId` (`WorktreeDetail.tsx`), which lists that worktree's terminal sessions via `GET .../terminals/` and renders them as dockview panels (see "Terminal arrangement" below).
7. A worktree row's Spotlight indicator/menu item calls `GET .../spotlight/` to show whether that worktree is the one currently mirrored into its repo's root, another worktree is (via `internal/spotlight.StatusForRoot`), or spotlight is unavailable (CLI not installed). "Start spotlight" calls `POST .../spotlight/start`, which shells out to the external `spotlight` CLI (`internal/spotlight.Start`, cwd = the worktree path) — a `409` means the root has uncommitted changes and the CLI itself refused, surfaced verbatim rather than retried. "Stop spotlight" calls `POST .../spotlight/stop`. See `docs/spotlight-sync.md` for what the underlying tool actually does (worktree → root mirroring, not the reverse — a design correction recorded there).
8. Each worktree's dirty/ahead-behind status comes from `GET .../worktrees/:id/status`, which runs `internal/gitops.Status` (`git status --porcelain=2 --branch`) and returns `{branch, dirty, has_upstream, ahead, behind}`. `ahead`/`behind` are only meaningful (and only rendered) when `has_upstream` is true — a freshly created worktree's branch typically has no upstream configured at all, in which case they're correctly not shown rather than misleadingly shown as `0`.

### Terminal arrangement (dockview)

- **`WorktreeDetail.tsx`** hosts a `DockviewReact` component (from `dockview-react`) instead of a manually-toggled tab strip. One dockview panel per terminal session, panel `id` == `TerminalSession.id`. The panel's component (`TerminalPanel`) is a one-line wrapper rendering the existing `<Terminal terminalId={...}/>` **unchanged** — dockview panels are just React components in the tree, so adding/splitting/resizing panels doesn't remount `Terminal.tsx` or touch its xterm/websocket lifecycle.
- **"+ New Terminal ▾" dropdown**, three actions, each first calls the existing `POST .../terminals/` to get a real `TerminalSession`, then places the panel via dockview's `addPanel({ position: { referencePanel, direction } })`:
  - **New tab** (`direction: 'within'`) — added as a tab within the currently active group; selecting it shows it full-size, sibling tabs hidden (classic tab-strip behavior, not a tile).
  - **Split right** (`direction: 'right'`) / **Split down** (`direction: 'below'`) — added as a new tile shown simultaneously alongside the reference panel. Resizable by dragging the boundary between tiles (dockview's native split-view behavior — not custom code).
- Closing a panel fires dockview's `onDidRemovePanel`, which calls the existing `DELETE .../terminals/:id` (real tmux kill) and drops it from local state.
- Each panel's `Terminal.tsx` opens its own `WebSocket` to `/ws/terminals/:id` (still one dedicated connection per terminal, not multiplexed — see `docs/session-persistence.md`'s documented simplification). Binary ws messages are raw bytes fed straight into the pty; text ws messages are JSON control frames, currently just `{"type":"resize","cols":N,"rows":N}`, sent on open and whenever xterm.js's fit addon recomputes size. The server (`handleTerminalWS`) runs `tmux attach-session -t <name>` under a pty via `creack/pty`. None of this changed by moving from a tab strip to dockview — see `docs/session-persistence.md` for the full tmux-restart-survival story.
- Styled by overriding dockview's `--dv-*` custom properties with Command Deck's tokens (`.command-deck-dockview` in `style.css`), starting from the built-in "abyss" theme as a base — not by fighting its DOM structure.
- **No OS-level popout into a separate browser window** — considered and explicitly ruled out for this tool, not deferred as a future TODO.
- **Layout persistence**: on mount, `GET .../layout` fetches the saved arrangement (a `404` means none saved yet, not an error); if present, `dockviewApi.fromJSON()` restores it, then `seedPanels` fills in any terminal that exists server-side but wasn't part of the saved layout (e.g. created after the last save). Every `onDidLayoutChange` (drag/resize/tab-switch) debounces a `PUT .../layout` of `dockviewApi.toJSON()` (500ms — the layout is one opaque JSON blob upserted wholesale, not worth writing on every single drag event). The server never inspects the layout's shape, just stores and returns it verbatim (`internal/api/layout.go`, `worktree_layouts` table). Verified surviving both a page reload and a full server kill+restart against a real running server.
- **Known simplification, not an oversight**: if a saved layout references a terminal id that no longer exists server-side (e.g. deleted through a path that didn't get a chance to save an updated layout first), that panel is left to fail gracefully at the websocket layer (`Terminal.tsx` shows a connection-error message) rather than this code hand-pruning dockview's serialized grid structure to remove it. A real edge case with a non-destructive fallback, not worth the complexity of generic grid-pruning logic.

### Per-worktree audit log viewer

- `GET /api/repos/:repoId/worktrees/:worktreeId/audit-log` (`internal/api/audit.go`) reads the whole global JSONL audit log (`internal/audit.Logger.ReadAll`, new — parses every line into a `map[string]any`, skipping malformed lines rather than failing the whole read) and filters to entries whose `worktree_id` field matches, newest first. There's still only ever one physical log file — this is a read-time filter, not a per-worktree file, since the whole log is small enough for a local tool to just scan on each request. Filtered on `worktree_id` alone, not also `repo_id`: worktree ids are already globally unique (`newID()`), and not every event type logs a `repo_id` alongside `worktree_id` (e.g. `terminal.create`/`terminal.close` only log `terminal_id`+`worktree_id`) — an earlier version of this handler required both and silently dropped those two event types as a result, caught during real-server verification, not by the test suite (the Go tests all happened to use event types that do carry `repo_id`).
- Deliberately does **not** require the worktree to still exist in the store — only the repo. The point of a per-worktree log is that it outlives the worktree itself (deletion, a killed terminal session): the work's history shouldn't disappear because the live DB row did. A worktree id with no matching entries (never existed, or existed and was deleted) returns `200 []`, not a `404`.
- Frontend: `WorktreeAuditLog.tsx`, a dialog fetching this endpoint and rendering entries with a friendly icon/label per known event type (`worktree.create/remove/archive/unarchive`, `terminal.create/close`, `spotlight.start/stop`, `claude.session.create`) plus a one-line summary (branch name, tab label, claude session id) and a `<details>` raw-JSON dump for anything else — new event types (e.g. a future `diff.comment_sent` from step 6's other half) render fine automatically via the fallback, no whitelist to maintain. Opened from: the kebab menu ("View worktree log", `WorktreeActionsMenu.tsx`, both `Sidebar.tsx` and `WorktreeList.tsx`) and a toolbar button inside `WorktreeDetail.tsx` for access without leaving the terminal view.

### Worktree lifecycle status (active / archived / deleted) and Archive

- `worktrees.status` (migrated in for existing DBs via `Store.migrateAddWorktreeStatus`, a `PRAGMA table_info` check + conditional `ALTER TABLE` — there's no migration framework in this project, see the plain `CREATE TABLE IF NOT EXISTS` schema, so a single added column doesn't warrant one). Values: `"active"` (default), `"archived"`, `"deleted"` (constants in `internal/store`).
- `POST .../archive` / `POST .../unarchive` (`internal/api/archive.go`) just flip this column — **no git operation at all**. This is the entire point of archive vs. delete: archiving hides a worktree from the normal list (`handleListWorktrees` now defaults to `status=active` only) without destroying the git worktree, its branch, or the ability to reopen a terminal in it later — including resuming a claude session recorded against it (see below). Both transitions are audit-logged (`worktree.archive`/`worktree.unarchive`).
- The kebab menu's former "Delete" item is now "Archive" (`WorktreeActionsMenu.tsx`) — real, destructive delete (`DELETE .../worktrees/:id`, unchanged: still runs `git worktree remove` for real) is no longer reachable from the per-worktree kebab at all, only from `worktreeActions.ts`'s `deleteWorktreeWithConfirm` (kept, currently unused by any UI surface).
- **`"deleted"` is a reserved status, not wired to any handler yet.** The plan (per direct user request) is a future settings-modal datagrid — bulk-select worktrees filtered by repo/status, then actually delete them. Soft-marking a row `"deleted"` while keeping it (rather than hard-removing, like today's delete endpoint does) would let that view show "what got cleaned up and when," but it collides with the `worktrees.path TEXT UNIQUE` constraint the instant someone creates a new worktree with the same name a soft-deleted row still occupies — left unresolved until the actual bulk-delete feature makes an informed call on that (e.g. mutate `path` on soft-delete, or drop the `UNIQUE` constraint).

### Claude session tracking (for future resume)

- `POST .../terminals/` accepts optional `claude_session_id`/`claude_session_title` fields. When present, `handleCreateTerminal` logs a `claude.session.create` audit event (`repo_id`, `worktree_id`, `terminal_id`, `claude_session_id`, `title`) in the same request that creates the terminal.
- The frontend (`createWorktreeWithClaudeTerminal` in `worktreeActions.ts`) generates the session id itself via `crypto.randomUUID()` **before** starting `claude`, rather than letting `claude` pick one — that's what makes it knowable to log at all. The auto-started command becomes `claude --session-id <uuid> -n <name>` (`--session-id`/`-n` are real `claude` CLI flags — verified via `claude --help`); `<name>` is the worktree's own adjective-noun name, already lowercase-alnum-and-hyphens only (`slugify`'s guarantee), so it's safe to embed directly in the command line typed into the pane's shell with no quoting.
- Why this matters independent of the terminal/tmux session's own lifecycle: the audit log entry (and thus the session id) survives the terminal being closed, the worktree being archived, or even the worktree being hard-deleted — restoring the exact same guarantee the rest of this audit-log feature already provides (see "Per-worktree audit log viewer" above). A person (or a future "Resume" button) can `cd` into the worktree path and run `claude --resume <id>` from that log entry at any later point, as long as the worktree itself hasn't actually been deleted.
- Not built yet: an actual "Resume claude session" UI action that reads the id from the log and does this automatically — today it's a manually-actionable record, not a one-click flow.

### Storage

- **SQLite** at `~/.worktree-studio/studio.db` via `modernc.org/sqlite` (pure Go, no cgo). Tables: `repos(id, name, path)`, `worktrees(id, repo_id, name, branch, path, created_at)`, `terminal_sessions(id, worktree_id, tmux_session_name, tab_label)`, and `worktree_layouts(worktree_id, layout_json, updated_at)` (one saved dockview layout per worktree, upserted wholesale — an opaque JSON blob this server never inspects the shape of).
- **`PRAGMA foreign_keys = ON`** is set once in `store.Open` (real fix, not always-been-true): SQLite does not enforce foreign keys — including every `ON DELETE CASCADE` in the schema above — unless this is set per connection, off by default. Verified empirically (a throwaway script: insert parent+child, delete parent, child row survives) that it was silently unenforced before this fix. Found while adding `worktree_layouts`; `internal/term`'s existing tests had to be fixed too, since they created terminal sessions referencing a worktree id that was never actually inserted — silently worked before, correctly rejected now.
- **Audit log** at `~/.worktree-studio/audit.log.jsonl`, one JSON object per line, written by `internal/audit.Logger.Log(event, fields)`. Every mutating handler in `internal/api` calls it (now including `terminal.create` / `terminal.close` / `spotlight.start` / `spotlight.stop`). See `docs/running-locally.md` for how to tail it.
- **Spotlight state** is NOT in worktree-studio's own store at all — the external `spotlight` CLI owns its own state file (which repo root is mirrored from which worktree, tracked by that tool, not queried here directly; `internal/spotlight.List`/`StatusForRoot` parse its `spotlight list` output instead). worktree-studio treats it as an external system to query, not data it's responsible for persisting.

### Frontend embedding

`cmd/worktree-studio/main.go` imports `worktree-studio/web`, a tiny package whose only job is holding `//go:embed all:dist` — go:embed patterns resolve relative to the *file* containing the directive, so the embed can't live in `cmd/worktree-studio` (a different directory from `web/dist`). `web/dist/.gitkeep` is checked into git so the directory (and thus the embed) always exists on a fresh checkout, even before `bun run build` has ever run — `go build ./...` works either way. At runtime, `mountFrontend` checks for a real `index.html` in the embedded FS; if it's missing (only the placeholder is there), it serves a small "run `bun run build`" HTML page instead of a blank/broken UI. Once a real build exists, static assets are served with an SPA fallback to `index.html` for client-side routes like `/repo/:id`.

### What's explicitly NOT built yet

Per `PLAN.md`'s build order, none of the following exist yet — don't assume they do:
- Monaco file editor (step 5)
- Git diff view + comment-to-agent (step 6 / TODO)
- Theme switching — Command Deck is the only theme (a recorded `PLAN.md` TODO, not built). OS-level popout for terminals is not a TODO at all — it was considered and ruled out.
- Terminal websocket connections are NOT multiplexed over one shared channel — each terminal has its own dedicated `/ws/terminals/:id` connection (a deliberate simplification vs. PLAN.md's originally-sketched single-multiplexed-channel design; see `docs/session-persistence.md`).
- Spotlight is a thin wrapper — it does NOT reimplement any sync logic, has no manifest/staging/lock scheme of its own (that was an earlier, wrong design; see `docs/spotlight-sync.md`'s "design correction" note), and there's no per-workspace/per-subdirectory scoping — it always mirrors an entire worktree into an entire root.
- The monitoring dashboard is REST-polled on a 5s interval, not pushed over a websocket — same simplification pattern as terminals/spotlight above, recorded in `PLAN.md`. There's no server-side push/broadcast mechanism anywhere in this codebase yet.

See `docs/running-locally.md` for how to run this step's server + frontend, and `.claude/skills/worktree-studio/SKILL.md` for the agent-facing usage doc.
